# Supabase project configuration
# Get these from Supabase Dashboard > Project Settings > API
# Never commit a real .env file with real values to version control.

VITE_SUPABASE_URL=https://gurrcfyttpssfwblrmpj.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_U2m-NG4LpxjJJKG9RsWfVg_HGL36GU5

# Optional: If unset, the app derives the QR base URL from window.location.origin at runtime.
# Set this if you want QR codes to always point at a fixed production domain regardless of
# where the admin generating them is currently logged in from.
VITE_PUBLIC_APP_URL=
